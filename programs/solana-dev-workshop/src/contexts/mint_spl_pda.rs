use crate::*;

/* Helpers
Sign your pda

let seeds = &[
            "seeds".as_bytes(),
            user_keypair.clone().to_bytes(),
            &[bump]
        ];
        let signer_seeds = &[&seeds[..]];
*/